# Creates an ask Input text field in streamlit app
# Stores the text variable
# Input is necessary, app should not run if value not added


def create_input_field():
    """
    TODO:
    Creates Input Field on the streamlit app
    Makes it compulsory to take that input
    """
    # Log the input created

    pass
